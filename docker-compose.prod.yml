version: '3.3'
services:
  dashboard:
    container_name: dashboard
    image: jerben/dashboard:0.4.4
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - HTTP_PORT=3000
      - ENV=production
    ports:
      - 3000:80
    command: ["java", "-jar", "dashboard-standalone.jar"]
